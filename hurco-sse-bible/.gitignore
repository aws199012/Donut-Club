node_modules/
backend/data/*.db
backend/data/*.db-journal
backend/data/*.db-wal
backend/data/*.db-shm
backend/uploads/*
!backend/uploads/.gitkeep
.env
dist/
frontend/public/hurco-logo-red.png

# Desktop (Electron) build outputs and staging copies — regenerated by
# desktop/prep.js and electron-builder on every build.
desktop/release/
desktop/bundled-backend/
desktop/frontend-dist/
# App icon stays local: it's the Hurco logo, a real brand asset, and this repo
# is public (same policy as the frontend watermark image).
desktop/build/
